# 从 baseline.yaml 继承单 Worker、单完整 GPU 的训练拓扑。该 Smoke Run
# 只减少 Epoch 数，不改变训练并行模型。
extends: baseline.yaml

run:
  role: smoke
  name_prefix: ray-cats-dogs-smoke

training:
  epochs: 1

ray:
  max_failures: 0
