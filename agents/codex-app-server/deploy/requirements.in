# External dependencies of galatea-mcp[platform] and galatea-codex-app-server[test].
# The two repository packages are built and installed as separately hashed wheels.
# Regenerate the platform lock and run pip check when either pyproject changes.
mcp==1.26.0
pydantic==2.12.5
jsonschema==4.25.1
cryptography==45.0.6
uvicorn==0.35.0
ray[default]==2.48.0
mlflow-skinny==3.14.0
boto3==1.40.0
httpx==0.28.1
